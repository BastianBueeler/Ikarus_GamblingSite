version: "3.7"

services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - 80:80
    volumes:
      - ./:/var/www/hmtl
  
  db:
    image: mariadb
    env_file:
      - .env
    volumes:
      - dbdata:/var/lib/mysql
      - ./init:/docker-entrypoint-initdb.d/

volumes:
  dbdata:
    driver: local
